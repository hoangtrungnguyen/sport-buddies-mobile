import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Widget tests require a running Supabase instance — add integration tests
    // as features are implemented.
    expect(true, isTrue);
  });
}
