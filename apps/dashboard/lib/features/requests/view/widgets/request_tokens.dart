import 'package:flutter/widgets.dart';

/// Status foreground tones, shared by the summary bar and the card badges.
const Color pendingFg = Color(0xFF854D0E);
const Color confirmedFg = Color(0xFF166534);
