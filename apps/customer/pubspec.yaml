name: customer
description: SportBuddies customer-facing Flutter app. Court booking marketplace for Ho Chi Minh City.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  spb_core:
    path: ../../packages/spb_core

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
